# 📱 Мобильная адаптация

## Что сделано:

### ✅ Адаптивная сетка
- Одна колонка на мобилке, две на десктопе
- Уменьшенные отступы на маленьких экранах
- Адаптивные размеры текста (text-lg sm:text-xl lg:text-2xl)

### ✅ Тач-события
Добавлены во всех интерактивных демо:
- **GameDemo** - прыжок по тапу
- **FlappyBirdDemo** - прыжок по тапу
- **ParticleDemo** - создание взрыва по тапу
- **GravityDemo** - добавление планеты по тапу

### ✅ Адаптивные кнопки
- Минимальный размер 44x44px для удобного тапа
- `touch-manipulation` для быстрого отклика
- `active:scale-95` для визуальной обратной связи

### ✅ Canvas адаптация
- `w-full h-auto` для автоматического масштабирования
- `touch-none` для предотвращения скролла при игре
- Координаты пересчитываются относительно размера canvas

### ✅ CSS оптимизации
- `-webkit-tap-highlight-color: transparent` - убирает синюю подсветку
- `-webkit-overflow-scrolling: touch` - плавный скролл
- Предотвращение выделения текста на интерактивных элементах

## Как тестировать:

### На компьютере:
1. Открой DevTools (F12)
2. Включи режим устройства (Ctrl+Shift+M)
3. Выбери iPhone или Android
4. Проверь все игры

### На реальном телефоне:
1. Узнай свой IP: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)
2. Открой на телефоне: `http://[твой-IP]:8080`
3. Убедись, что телефон в той же WiFi сети

## Что работает на мобилке:

✅ Все игры управляются тапом
✅ Canvas масштабируется под экран
✅ Кнопки удобного размера
✅ Плавная анимация
✅ Нет случайного зума
✅ Нет синей подсветки при тапе

## Известные ограничения:

⚠️ Snake Game - управление только клавиатурой (можно добавить виртуальные кнопки)
⚠️ WebGL Cube - на слабых телефонах может тормозить
⚠️ Matrix Rain - много символов могут замедлить старые устройства

## Рекомендации:

- Тестируй на реальных устройствах
- Проверь на iOS и Android
- Убедись, что все тапы работают
- Проверь производительность на слабых телефонах
